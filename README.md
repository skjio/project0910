## ctrl+shift+v 預覽
### git add . 所有檔案到暫存區 git commit -m "新增訊息" 到儲存區
### git pull origin main 測試(在本地上傳到遠端之前) github
### git push origin main
### git checkout <分支> 切換分支
## 從雲端更新版本
### git pull 包含 git fetch 和git merge origin main


## 撤銷功能三劍客 revert、reset、restore
### revert 把commit_3的更動復原 建立commit_4取代3
### reset commit3刪除 回到工作目錄 統一

### git tag -a v1.0 -m "發布版本1.0"